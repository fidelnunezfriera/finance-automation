# Dependencias del proyecto completo (pipeline + dashboard).
# Instalación:  pip install -r requirements.txt
#
# Probado con Python 3.11-3.13.

# --- Pipeline: exportación, limpieza y carga en Google Sheets ---
# [playwright]: pytr lo necesita para el login web (teléfono+PIN) de Trade
# Republic -- sin el extra, la exportación falla con "playwright is not
# installed". El binario del navegador (Chromium) lo instala setup.bat aparte
# con `playwright install chromium`, pip solo trae el paquete Python.
pytr[playwright]>=0.2.0
pandas>=2.0.0
pyyaml>=6.0.1
gspread>=6.0.0
google-auth>=2.29.0

# --- Dashboard Streamlit (app/) ---
streamlit>=1.51.0        # width="stretch" (sustituye a use_container_width)
plotly>=5.22.0
numpy>=1.26.0
scikit-learn>=1.4.0
statsmodels>=0.14.0      # SARIMA y Holt-Winters del banco de evaluación
lightgbm>=4.0.0          # gradient boosting del banco de evaluación
yfinance>=0.2.40
requests>=2.31.0

# --- Clasificador de categorías (pipeline/entrenar_clasificador.py) ---
joblib>=1.3.0             # persistencia de los modelos entrenados
